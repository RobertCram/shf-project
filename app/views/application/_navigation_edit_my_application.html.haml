%li.menu-item
  - if policy(membership_app).update?
    = link_to t('menus.nav.users.my_application'), edit_shf_application_path(membership_app)
  - else
    = link_to t('menus.nav.users.my_application'), shf_application_path(membership_app)
